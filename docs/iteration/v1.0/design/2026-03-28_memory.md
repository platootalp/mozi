# Memory Module (记忆模块)

## 文档信息

| 字段 | 内容 |
|------|------|
| 模块名称 | Memory |
| 职责 | Working/Short-term/Long-term 记忆 |
| 路径 | `mozi/storage/memory/` |
| 文档版本 | v1.0 |
| 状态 | 规划中 |
| 创建日期 | 2026-03-28 |

---

## 1. 三层记忆系统

| 层级 | 存储 | 生命周期 | 用途 |
|------|------|----------|------|
| Working | 内存 | 会话内 | LLM Context 滑动窗口 |
| Short-term | Qdrant | 180 天 | 语义搜索、RAG |
| Long-term | SQLite | 永久 | 用户偏好、历史经验 |

## 2. Memory Store

```python
# 三层记忆系统接口
class MemoryStore:
    # Working Memory - 内存中的滑动窗口
    async def get_working(self, session_id: str) -> WorkingMemory: ...
    async def update_working(self, session_id: str, memory: WorkingMemory) -> None: ...

    # Short-term Memory - Qdrant 向量存储
    async def search(self, query: str, limit: int = 5) -> List[MemoryEntry]: ...
    async def store(self, entry: MemoryEntry) -> None: ...

    # Long-term Memory - SQLite 持久化
    async def get_preferences(self, session_id: str) -> Preferences: ...
    async def store_experience(self, experience: Experience) -> None: ...
```

## 3. 依赖关系

- **依赖模块**: Storage Layer
- **被依赖模块**: Agent Layer, Context Module

---

## 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-03-28 | 初始版本，从架构文档拆分 |
