# Agent Module (智能体层)

## 文档信息

| 字段 | 内容 |
|------|------|
| 模块名称 | Agent |
| 职责 | 内置 Agent、扩展 Agent |
| 路径 | `mozi/agent/` |
| 文档版本 | v1.0 |
| 状态 | 规划中 |
| 创建日期 | 2026-03-28 |

---

## 1. 模块概述

Agent 模块是系统的执行核心，负责实际的任务执行。每个 Agent 是一个独立的执行单元，具备自己的思考和执行能力。Agent 层不追求分层结构，本质是模块串连协作。

### 1.1 核心职责

- 任务执行 (think + act)
- 工具调用
- 结果返回
- 状态管理

### 1.2 差异化特性

| 特性 | 说明 |
|------|------|
| ReAct 循环 | Reasoning + Acting 模式 |
| 工具动态绑定 | 每个 Agent 可绑定不同工具 |
| 可扩展 | 支持自定义 Agent |

---

## 2. 模块结构

```
mozi/agent/
├── __init__.py
├── base.py              # Agent 基类
├── registry.py          # Agent 注册表
│
├── builtin/             # 内置 Agent
│   ├── __init__.py
│   ├── builder.py       # Builder Agent
│   ├── reviewer.py      # Reviewer Agent
│   ├── explorer.py      # Explorer Agent
│   ├── planner.py       # Planner Agent
│   └── researcher.py    # Researcher Agent
│
└── runtime/             # Agent 运行时
    ├── executor.py      # 执行器
    └── state.py         # 状态管理
```

---

## 3. BaseAgent 抽象基类

### 3.1 接口定义

```python
class BaseAgent(ABC):
    """Agent 基类，所有 Agent 必须继承此类"""

    @property
    @abstractmethod
    def name(self) -> str:
        """Agent 名称"""
        pass

    @property
    @abstractmethod
    def description(self) -> str:
        """Agent 描述"""
        pass

    @property
    def tools(self) -> List[Tool]:
        """Agent 可用的工具列表"""
        return []

    @property
    def supported_intents(self) -> List[Intent]:
        """支持的意图类型"""
        return []

    @abstractmethod
    async def think(self, task: Task, context: Context) -> Thought:
        """
        思考阶段：分析任务，决定行动
        - 输入：任务、上下文
        - 输出：思考结果（包含行动计划）
        """
        pass

    @abstractmethod
    async def execute(self, thought: Thought) -> Result:
        """
        执行阶段：执行思考决定的行动
        - 输入：思考结果
        - 输出：执行结果
        """
        pass

    async def run(self, task: Task, context: Context) -> AgentRunResult:
        """
        完整的 Agent 运行流程（think + execute）
        """
        thought = await self.think(task, context)
        result = await self.execute(thought)
        return AgentRunResult(thought=thought, result=result)
```

### 3.2 Agent 状态

```python
class AgentState(Enum):
    IDLE = "idle"              # 空闲
    THINKING = "thinking"      # 思考中
    EXECUTING = "executing"    # 执行中
    WAITING = "waiting"        # 等待（工具执行）
    DONE = "done"              # 完成
    ERROR = "error"            # 错误
```

---

## 4. 内置 Agent 详解

### 4.1 Builder Agent

**职责**: 编写和修改代码

**绑定工具**: 全部内置工具

| 工具 | 用途 |
|------|------|
| read | 读取文件内容 |
| write | 创建新文件 |
| edit | 修改文件内容 |
| bash | 执行 shell 命令 |
| glob | 查找文件 |
| grep | 搜索内容 |

**功能点**:

| 功能 | 说明 |
|------|------|
| 代码生成 | 根据描述生成代码 |
| 代码修改 | 理解需求修改代码 |
| 文件操作 | 创建/读取/编辑文件 |
| 命令执行 | 运行构建/测试命令 |

**使用场景**:
- 直接修改代码
- 创建新功能
- 修复 bug
- 重构代码

### 4.2 Reviewer Agent

**职责**: 审查代码质量

**绑定工具**: read, grep, glob

| 工具 | 用途 |
|------|------|
| read | 读取代码文件 |
| grep | 搜索代码模式 |
| glob | 查找相关文件 |

**功能点**:

| 功能 | 说明 |
|------|------|
| 代码审查 | 检查代码质量 |
| 问题发现 | 识别潜在 bug |
| 风格检查 | 代码风格一致性 |
| 性能分析 | 识别性能问题 |

**使用场景**:
- PR 审查
- 代码质量评估
- 安全漏洞检测

### 4.3 Explorer Agent

**职责**: 探索代码库结构

**绑定工具**: read, grep, glob

**功能点**:

| 功能 | 说明 |
|------|------|
| 结构分析 | 分析代码库结构 |
| 依赖追踪 | 追踪模块依赖 |
| 关系发现 | 发现组件间关系 |
| 文档生成 | 生成代码文档 |

**使用场景**:
- 了解代码库
- 追踪 bug 来源
- 分析架构

### 4.4 Planner Agent

**职责**: 规划复杂任务

**绑定工具**: read, grep, glob

**功能点**:

| 功能 | 说明 |
|------|------|
| 任务分解 | 将大任务分解为小任务 |
| 依赖分析 | 分析任务间依赖 |
| 排序优化 | 优化任务执行顺序 |
| 资源规划 | 估算所需资源 |

**使用场景**:
- 复杂任务规划
- 项目结构设计
- 迁移计划

### 4.5 Researcher Agent

**职责**: 技术研究

**绑定工具**: read, web_search

**功能点**:

| 功能 | 说明 |
|------|------|
| 技术调研 | 研究技术方案 |
| 方案对比 | 对比不同方案 |
| 文档总结 | 总结调研结果 |
| 建议生成 | 生成技术建议 |

**使用场景**:
- 技术选型
- 方案调研
- 学习新技仓

---

## 5. 扩展 Agent

### 5.1 加载机制

从 `~/.mozi/agents/` 目录加载自定义 Agent。

```python
class AgentRegistry:
    def __init__(self):
        self._builtin_agents: Dict[str, BaseAgent] = {}
        self._external_agents: Dict[str, BaseAgent] = {}

    def load_builtin_agents(self) -> Dict[str, BaseAgent]:
        """加载内置 Agent"""
        return {
            "builder": BuilderAgent(),
            "reviewer": ReviewerAgent(),
            "explorer": ExplorerAgent(),
            "planner": PlannerAgent(),
            "researcher": ResearcherAgent(),
        }

    def load_external_agents(self) -> Dict[str, BaseAgent]:
        """从 ~/.mozi/agents/ 加载外部 Agent"""
        agents = {}
        agents_dir = Path("~/.mozi/agents/").expanduser()

        if not agents_dir.exists():
            return agents

        for path in agents_dir.glob("*.py"):
            if path.name.startswith("_"):
                continue
            agent = self._load_agent_module(path)
            if agent:
                agents[agent.name] = agent

        return agents

    def _load_agent_module(self, path: Path) -> Optional[BaseAgent]:
        """加载单个 Agent 模块"""
        module_name = path.stem
        spec = importlib.util.spec_from_file_location(module_name, path)
        if not spec or not spec.loader:
            return None

        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)

        for item in dir(module):
            obj = getattr(module, item)
            if isinstance(obj, type) and issubclass(obj, BaseAgent) and obj != BaseAgent:
                return obj()
        return None

    def get_agent(self, name: str) -> Optional[BaseAgent]:
        """获取 Agent 实例"""
        return self._builtin_agents.get(name) or self._external_agents.get(name)

    def list_agents(self) -> List[str]:
        """列出所有可用 Agent"""
        return list(self._builtin_agents.keys()) + list(self._external_agents.keys())
```

### 5.2 自定义 Agent 示例

```python
# ~/.mozi/agents/my_agent.py
from mozi.agent import BaseAgent

class MyCustomAgent(BaseAgent):
    @property
    def name(self) -> str:
        return "my-agent"

    @property
    def description(self) -> str:
        return "My custom agent for specific tasks"

    @property
    def tools(self) -> List[Tool]:
        return [ReadTool(), GrepTool()]

    async def think(self, task: Task, context: Context) -> Thought:
        # 实现思考逻辑
        return Thought(...)
```

---

## 6. 意图路由

```
┌─────────────────────────────────────────────────────────────┐
│                    Intent → Agent Routing                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  EXPLORE  ──────────────────────────────────→  Explorer     │
│                                                             │
│  CODE    ──────────────────────────────────→  Builder       │
│                                                             │
│  REVIEW  ──────────────────────────────────→  Reviewer      │
│                                                             │
│  PLAN    ──────────────────────────────────→  Planner       │
│                                                             │
│  RESEARCH ─────────────────────────────────→  Researcher    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 核心工作流

### 7.1 Agent ReAct 流程

```
任务输入
    │
    ▼
┌─────────────────────────────────────────┐
│              THINK (思考)                 │
│                                          │
│  1. 理解任务                             │
│  2. 检查上下文                           │
│  3. 决定行动                             │
│  4. 生成工具调用计划                      │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│              ACT (执行)                   │
│                                          │
│  1. 调用工具                             │
│  2. 获取结果                             │
│  3. 更新上下文                           │
└──────────────────┬──────────────────────┘
                   │
                   ▼
            任务完成? ── 否 ──→ 继续 THINK
                   │
                   是
                   ▼
              返回结果
```

---

## 8. 依赖关系

- **依赖模块**: Model Layer, Tools Layer, Memory Layer
- **被依赖模块**: Orchestration Layer

---

## 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-03-28 | 初始版本，从架构文档拆分 |
