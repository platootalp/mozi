# AI Coding Agent 系统架构设计

## 1. 系统整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              接入层 (Ingress)                            │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────────────┐ │
│  │   CLI   │  │  Web UI │  │  API    │  │  IDE    │  │  MCP Client     │ │
│  │  Typer  │  │ FastAPI │  │ Gateway │  │Extension│  │  (外部集成)     │ │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘  └────────┬────────┘ │
│       └─────────────┴─────────────┴─────────────┴────────────────┘        │
│                                    ↓                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                           配置管理层 (Config)                            │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  config.json │ agents.json │ tools.json │ mcp.json │ skills.json   │ │
│  │  user.json (本地覆盖)                                               │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                    ↓                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                          编排层 (Orchestrator)                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐ │
│  │  意图识别   │  │ 复杂度评估  │  │  任务路由器 │  │   会话管理层    │ │
│  │  需求澄清   │→ │  策略选择   │→ │  执行调度   │→ │   Session Mgr   │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────┘ │
│                                    ↓                                     │
│                   ┌────────────────┴────────────────┐                   │
│                   ↓                                 ↓                   │
│        ┌─────────────────────┐          ┌─────────────────────┐         │
│        │    简单任务通道      │          │    复杂任务通道      │         │
│        │  (单Agent FastPath) │          │  (多Agent协作)       │         │
│        │                     │          │                     │         │
│        │  ┌───────────────┐ │          │  ┌───────────────┐  │         │
│        │  │  Agent实例    │ │          │  │  DAG调度器    │  │         │
│        │  │  ReAct循环    │ │          │  │  Task拆分     │  │         │
│        │  │  直接执行     │ │          │  │  Subagent委派 │  │         │
│        │  └───────────────┘ │          │  │  结果聚合     │  │         │
│        └─────────────────────┘          │  └───────────────┘  │         │
│                                         └─────────────────────┘         │
├─────────────────────────────────────────────────────────────────────────┤
│                          能力层 (Capabilities)                           │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────────────────────┐ │
│  │   MCP接入层   │  │  Skills引擎   │  │         工具框架               │ │
│  │               │  │               │  │  ┌─────┐ ┌─────┐ ┌─────────┐  │ │
│  │ • stdio/SSE   │  │ • 工作流编排  │  │  │Bash │ │File │ │ LSP     │  │ │
│  │ • HTTP/WS     │  │ • 条件分支    │  │  ├─────┤ ├─────┤ ├─────────┤  │ │
│  │ • 健康检查    │  │ • 参数模板    │  │  │Web  │ │Grep │ │ AST     │  │ │
│  │ • 权限协商    │  │ • 版本管理    │  │  ├─────┤ ├─────┤ ├─────────┤  │ │
│  │               │  │               │  │  │MCP  │ │Git  │ │ Browser │  │ │
│  └───────────────┘  └───────────────┘  │  └─────┘ └─────┘ └─────────┘  │ │
│                                        └───────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                    安全与权限层 (Security)                          │ │
│  │  工具白名单 │ 敏感操作审批(HITL) │ 沙箱隔离 │ 审计日志 │ 哈希锚定  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────────┤
│                            存储层 (Storage)                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐ │
│  │    热数据   │  │    温数据   │  │    冷数据   │  │    归档数据     │ │
│  │   (内存)    │→ │  (向量库)   │→ │  (SQLite)   │→ │   (文件系统)    │ │
│  │             │  │             │  │             │  │                 │ │
│  │ 会话上下文  │  │ [云端]      │  │ 结构化数据  │  │ 历史会话        │ │
│  │ Agent状态   │  │ Qdrant/     │  │ 任务状态    │  │ 日志归档        │ │
│  │ 工具缓存    │  │ Milvus      │  │ 调用记录    │  │ 审计记录        │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 配置层 Schema

### 2.1 文件组织

```
.mozi/                          # 项目级配置
├── config.json                 # 系统核心配置
├── agents.json                 # Agent注册表
├── tools.json                  # 工具策略配置
├── mcp.json                    # MCP服务配置
├── skills.json                 # Skill注册表
├── agents/                     # Agent提示词文档 (*.md)
├── skills/                     # Skill工作流文档 (*.md)
├── rules/                      # 项目规则 (*.md)
└── memory/                     # 持久化记忆 (*.md)

~/.mozi/                        # 用户全局配置
├── user.json                   # 用户偏好覆盖
├── agents/                     # 自定义Agent
└── skills/                     # 自定义Skill
```

### 2.2 config.json Schema

| 字段 | 类型 | 说明 |
|------|------|------|
| `server` | Object | 服务端配置（host, port, ssl） |
| `storage` | Object | 存储层配置（SQLite路径、向量库连接等） |
| `security` | Object | 安全策略（审批规则、沙箱模式） |
| `features` | Object | 功能开关（实验性功能启用） |
| `logging` | Object | 日志配置（级别、输出、轮转策略） |

### 2.3 agents.json Schema

| 字段 | 类型 | 说明 |
|------|------|------|
| `agents` | Array | Agent定义列表 |
| `agents[].name` | String | Agent唯一标识 |
| `agents[].description` | String | 调度描述（用于意图匹配） |
| `agents[].mode` | Enum | `primary` / `subagent` / `hidden` |
| `agents[].prompt` | String | Markdown提示词文件路径 |
| `agents[].model` | Object | 模型配置（provider, model, temperature） |
| `agents[].permissions` | Object | 工具权限（read/edit/bash/webfetch/task） |
| `agents[].tools` | Array | 显式工具白名单 |
| `agents[].mcp_servers` | Array | 关联的MCP服务名称 |
| `agents[].max_steps` | Number | 单会话最大步数限制 |
| `agents[].timeout` | Number | 单次调用超时（秒） |
| `default_agent` | String | 默认使用的Agent名称 |
| `complexity_threshold` | Object | 复杂度评估阈值配置 |

### 2.4 tools.json Schema

| 字段 | 类型 | 说明 |
|------|------|------|
| `policies` | Object | 全局工具策略 |
| `policies.*.default` | Enum | `allow` / `deny` / `ask` |
| `allowlist` | Array | 命令白名单（支持通配符） |
| `sandbox` | Object | 沙箱配置（mode, image, resources） |

### 2.5 mcp.json Schema

| 字段 | 类型 | 说明 |
|------|------|------|
| `servers` | Object | MCP服务配置映射 |
| `servers.*.transport` | Enum | `stdio` / `sse` / `http` / `websocket` |
| `servers.*.command` | String | stdio模式下的启动命令 |
| `servers.*.url` | String | 网络模式下的服务端点 |
| `servers.*.env` | Object | 环境变量配置 |
| `servers.*.permissions` | Object | 该Server的工具权限覆盖 |

### 2.6 skills.json Schema

| 字段 | 类型 | 说明 |
|------|------|------|
| `scan_paths` | Array | Skill文档扫描路径列表 |
| `auto_load` | Boolean | 是否自动加载所有发现的Skill |
| `triggers` | Object | 触发词到Skill的映射 |

---

## 3. 编排层组件

### 3.1 意图识别器

**职责**：解析用户输入，识别任务类型和意图，检查需求完整性

**输入**：用户原始输入 + 当前会话上下文

**输出**：结构化意图描述 + 完整性评估 + 澄清问题（如有）

### 3.2 复杂度评估引擎

**评估维度**：

| 维度 | 说明 | 阈值 |
|------|------|------|
| 预估修改文件数 | 根据意图推断需要修改的文件数量 | >3 → 复杂 |
| 技术栈多样性 | 涉及的技术栈种类数 | >2 → 复杂 |
| 跨模块依赖 | 是否需要修改多个模块/服务 | 是 → 复杂 |
| 历史成功率 | 相似历史任务的完成成功率 | <70% → 复杂 |
| 代码库规模 | 目标代码库的文件/行数规模 | 大规模 → 权重增加 |

**输出**：`SIMPLE` / `MEDIUM` / `COMPLEX` 复杂度等级

### 3.3 任务路由器

**路由策略**：

| 复杂度 | 执行通道 | 说明 |
|--------|----------|------|
| SIMPLE | 单Agent FastPath | 直接实例化Builder Agent执行 |
| MEDIUM | 单Agent增强监控 | 单Agent执行，增加步骤限制和监控 |
| COMPLEX | 多Agent协作 | 生成DAG，Orchestrator调度多Subagent |

### 3.4 会话管理层

**生命周期管理**：

| 操作 | 说明 |
|------|------|
| create | 创建新会话，分配ID和初始上下文 |
| resume | 恢复已有会话，加载历史状态 |
| fork | 从父会话创建子会话（用于并行Subagent） |
| archive | 归档会话，数据移至冷存储 |

**上下文压缩策略**：

- **触发条件**：上下文Token数接近模型上限的80%
- **压缩动作**：
  1. 生成历史对话摘要
  2. 长工具结果卸载到文件系统
  3. 触发记忆冲刷，Agent写入MEMORY.md

---

## 4. 能力层组件

### 4.1 MCP接入层

**支持传输协议**：

| 协议 | 适用场景 | 说明 |
|------|----------|------|
| stdio | 本地工具 | 启动子进程，stdin/stdout通信 |
| sse | 远程服务 | Server-Sent Events单向流 |
| http | 远程服务 | HTTP streaming双向流 |
| websocket | 实时交互 | 全双工长连接 |

**能力协商流程**：

1. 建立传输连接
2. 发送初始化握手
3. 接收Server能力声明（Tools/Resources/Prompts/Roots）
4. 根据权限策略过滤可用能力
5. 注册到工具框架

### 4.2 Skills引擎

**Skill文档结构**（Markdown）：

```markdown
---
name: skill-name
description: Skill功能描述
tags: [tag1, tag2]
triggers: ["触发词1", "触发词2"]
---

# Skill名称

## 适用场景
何时使用该Skill

## 执行步骤
1. 步骤一
2. 步骤二
   - 子步骤

## 检查清单
- [ ] 检查项1
- [ ] 检查项2

## 示例
代码示例或输出样例
```

**执行流程**：

1. 解析Frontmatter元数据
2. 匹配触发条件
3. 参数模板填充
4. 按步骤执行（可能涉及工具调用）
5. 检查清单验证
6. 返回结果

### 4.3 工具框架

**内置工具列表**：

| 工具 | 类型 | 权限要求 | 说明 |
|------|------|----------|------|
| read | 文件 | allow | 读取文件内容 |
| write | 文件 | ask | 写入新文件 |
| edit | 文件 | ask | 基于哈希锚定的安全编辑 |
| bash | 执行 | ask | 执行shell命令 |
| grep | 搜索 | allow | 代码内容搜索 |
| glob | 搜索 | allow | 文件模式匹配 |
| lsp | 语义 | allow | LSP服务器查询 |
| ast_grep | 语义 | allow | AST语法树查询 |
| web_search | 外部 | ask | 网络搜索 |
| web_fetch | 外部 | ask | 网页抓取 |
| task | 编排 | allow | 委派子任务（调用其他Agent） |

**哈希锚定编辑机制**：

解决多Agent并发编辑冲突，每处编辑包含：
- 行号标识（LINE#ID）
- 原始内容哈希
- 期望替换内容

编辑前校验锚点是否匹配，不匹配则返回冲突信息。

---

## 5. 安全与权限层

### 5.1 四层安全防护

| 层级 | 机制 | 说明 |
|------|------|------|
| 沙箱隔离 | Sandbox | 工具执行环境隔离（off/non-main/all） |
| 工具策略 | Tool Policy | 工具级权限控制（allow/deny/ask） |
| 审批机制 | HITL | 敏感操作人工介入确认 |
| 审计日志 | Audit | 全量操作记录与追溯 |

### 5.2 权限层级（deny优先）

1. 全局默认策略
2. Agent级策略覆盖
3. 工具特定策略
4. 沙箱内策略
5. 单次调用覆盖

### 5.3 敏感操作类型

- bash命令执行
- 文件覆盖写入（特别是配置文件）
- MCP工具调用
- 网络请求（web_search/web_fetch）

---

## 6. 存储层

### 6.1 三级存储架构

| 层级 | 载体 | 数据类型 | 持久性 | 访问延迟 |
|------|------|----------|--------|----------|
| 热 | 内存 | 当前会话上下文、Agent状态、工具缓存 | 会话级 | 极低 |
| 温 | 云端向量库 | 语义向量、代码嵌入、记忆索引 | 持久 | 低 |
| 冷 | SQLite | 结构化数据、任务状态、调用记录 | 持久 | 中 |
| 归档 | 文件系统 | 历史会话、日志归档、大文件卸载 | 持久 | 高 |

**自动分层策略**：

- 热→温：会话结束或超过时间阈值
- 温→冷：向量访问频率降低
- 冷→归档：超过保留策略期限

### 6.2 双轨检索策略

| 场景 | 策略 | 工具 |
|------|------|------|
| 工作区内 | 精准检索 | grep/glob/ast_grep |
| 跨仓库 | 语义检索 | 向量库相似度搜索 |
| 混合 | 混合排序 | 结合精准命中和语义相似度 |

---

## 7. 部署架构

```
┌─────────────────────────────────────────────────────────┐
│                    本地部署 (Local)                      │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Core Engine                                    │   │
│  │  - Orchestrator                                │   │
│  │  - Agent Runtime                               │   │
│  │  - Tool Framework                              │   │
│  │  - Skills Engine                               │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Local Storage                                  │   │
│  │  - SQLite (结构化数据)                          │   │
│  │  - File System (文档/归档)                      │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                            │
                            ↓ 可选连接
┌─────────────────────────────────────────────────────────┐
│              云端服务 (Cloud Services)                   │
│  ┌─────────────────────┐  ┌─────────────────────────┐  │
│  │  Vector Database    │  │  Model API              │  │
│  │  (Qdrant/Milvus)    │  │  (OpenAI/Claude/Ollama) │  │
│  └─────────────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

**混合模式说明**：

- 核心引擎本地运行，确保代码安全和低延迟
- 向量库存储可配置为本地或云端，支持大规模语义检索
- 模型API可配置多提供商，支持本地Ollama或云端服务

---

## 8. 数据流与交互序列

### 8.1 简单任务执行流

```
User → CLI → Intent识别 → 复杂度评估(SIMPLE) → 实例化Builder Agent
                                                        ↓
User ← 结果返回 ← 工具调用结果 ← ReAct循环 ← 任务执行完成
```

### 8.2 复杂任务多Agent协作流

```
User → 需求澄清 → 复杂度评估(COMPLEX) → 生成DAG计划
                                              ↓
                    ┌─────────────────────────┼─────────────────────────┐
                    ↓                         ↓                         ↓
              [Explorer]               [Builder A]               [Builder B]
              代码库探索                实现模块A                 实现模块B
                    └─────────────────────────┬─────────────────────────┘
                                              ↓
                                        [Reviewer]
                                        代码审查
                                              ↓
                                        [Orchestrator]
                                        结果聚合 → 返回用户
```

### 8.3 上下文压缩与记忆冲刷

```
上下文接近阈值 ──→ 触发压缩 ──┬──→ 生成摘要
                            ├──→ 长文本卸载到文件
                            └──→ 记忆冲刷提醒 → Agent写入MEMORY.md
```

---

## 9. 与其他系统的对比定位

| 维度 | Mozi | Cursor | OpenCode | OpenClaw |
|------|------|--------|----------|----------|
| **部署模式** | 混合（本地+云端） | 云端IDE | 本地CLI | 本地CLI |
| **规划模式** | 自适应（基于复杂度） | Plan Mode显式 | Plan/Build分离 | ReAct隐式 |
| **多Agent** | Orchestrator调度 | Subagents | Task工具层级 | Sub-agent Lane |
| **配置格式** | JSON + Markdown | Rules(.mdc) | JSON/Markdown | YAML/Markdown |
| **工具扩展** | MCP + 内置 | MCP | 内置 + MCP | 插件系统 |
| **记忆系统** | 三级存储+双轨检索 | 云端Knowledge Base | 插件依赖 | 本地Markdown |
| **安全模型** | 四层防护+HITL | 默认审批 | 权限配置 | 沙箱+策略 |

---

*文档版本: 1.0*
*更新日期: 2026-03-25*
