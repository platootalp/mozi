"""Entry point for running mozi as a module.

Usage:
    python -m mozi

This module serves as the main entry point when mozi is invoked via the
Python module interface.
"""

from __future__ import annotations


def main() -> None:
    """Main entry point for the mozi agent.

    This function is called when running `python -m mozi`.
    Currently a placeholder for future CLI implementation.
    """
    print("Mozi AI Coding Agent v0.1.0")
    print("Run 'mozi --help' for usage information.")


if __name__ == "__main__":
    main()
